# Add these lines to your .env file to enable auth testing

# Auth System (v2.2+)
# Set to 'true' to enable new auth system, 'false' or omit to use legacy username system
VITE_AUTH_ENABLED=false

# Auth Provider Selection (only used if VITE_AUTH_ENABLED=true)
# Options: mock, clerk, supabase, auth0
VITE_AUTH_PROVIDER=mock

# Clerk Configuration (only needed if using Clerk - coming soon)
# VITE_CLERK_PUBLISHABLE_KEY=your_clerk_key_here

